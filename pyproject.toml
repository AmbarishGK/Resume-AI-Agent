[tool.ruff]
line-length = 100
target-version = "py39"
exclude = [
  ".venv",
  "app/data",
  "rag_resume_chroma",
  "__pycache__",
]

[tool.ruff.lint]
# Keep linting lightweight so it’s useful without turning into a refactor project.
select = ["E", "F", "I"]

[tool.ruff.lint.isort]
known-first-party = ["marnow", "tools"]
