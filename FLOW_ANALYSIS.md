# Flow notes (archived)

This is an older scratchpad doc.

If you’re looking for:
- how to run the repo → `README.md`
- pipeline steps + flags → `WORKFLOW.md`

The pipeline is still: scrape → fetch JDs → ingest → (optional) match.
